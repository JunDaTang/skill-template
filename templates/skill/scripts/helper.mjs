#!/usr/bin/env node
<!--
  scripts/ 下放「每次触发都要重写的重复性小工具」，让 skill 引用而不是重造。
  要求：零依赖（只用 Node 内置模块），加 --help，退出码表达成败。
  引用方式（写在 SKILL.md 正文里）：
    Run `node scripts/check-thing.mjs <arg>` to ...
-->
console.log("TODO: implement {{name}} helper script");
